// 1. Integer type and decimal
const integer = 10
const decimal = 3.14
console.log(typeof integer, typeof decimal)

// 2. Scientific notation
const scientific = 5e3

// 3. Infinity and NaN
const astromic = Infinity
const notNumber = NaN

// Artithmetic Operations
// 1. Add, Substraction, Multiply & Division
const sum = 1 + 1
const rest = 2 - 1
const multiply = 2 * 2
const division = 4 / 2

// 2. Module and exponent
const module = 15 % 8
const exponent = 2 ** 3

// Presition
const result = 0.1 + 0.2
console.log(result)
console.log(result.toFixed(1))
console.log(result === 0.3)

// Advanced operations
const squareRoot = Math.sqrt(3)
const absoluteValue = Math.abs(-7)
const randomNumber = Math.random()
console.log(squareRoot)
console.log(absoluteValue)
console.log(randomNumber)