// Primitives and complex

// Primitives
let name = 'Kyb3r'  // string
let age = 17  // number
let isLegal = false // boolean
let empty = null    // normally we use this for describe empty value
let noDefinido = undefined  // normally we don't use this, normally js give

let simbo = Symbol('unique')
let bigNumber = 2n  // bigint

// Complex
let car = {
    brand: 'Tesla',
    model: 'X'
}   // Object

let fruits = ['Apple', 'Strawberries', 'Pineapple'] // array

function sayHello(name) {
    console.log("Hello '" + name + "'");
}   // Function
sayHello("Kyb3r")