
```javascript
// Arrays:
// !user || !userList
const users = ["Alex", "Mariana", "Jessica"];
const userNames = ["Alex", "Mariana", "Jessica"];

// Booelans:
const isValid = true;
const hasColor = true;
const canRead = false;

// Numbers: (recommend prefix: max, min, total)
// !users
const maxUsers = 50;
const minUsers = 10;
const totalUsers = 15;

// Functions: (recommend verb: create, update, send, get) [verb, sustantive]
// !explicit like: createUserIfNotExists() or updateUserIfNotEmpty()
createUser();
updateUser();
sendEmail();
// Acces:
getUser();
// Modification:
setUser()
// Predicate:
isValidUser();

// Classes: (recommend sustantives)
// !generic names like: data, info, manager
class User {}
class UserProfile {}
class Account {}
```