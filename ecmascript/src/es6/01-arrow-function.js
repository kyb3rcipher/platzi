function square(number) {
    return number * number;
}

// Recomendation: use const for arrow functions (not var or let)
const square = (number) => {
    return number * number;
}

const square = num => num * num;